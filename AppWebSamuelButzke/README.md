# AppWebSamuelButzke

Aplicação Blazor Web App com componentes interativos no servidor, acesso MySQL para processos e recursos estáticos incluídos.

## Execução

```bash
dotnet restore
dotnet run
```

A conexão MySQL é lida de `ConnectionStrings:MySqlConnection` em `appsettings.json`.
